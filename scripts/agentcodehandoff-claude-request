#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "usage: agentcodehandoff-claude-request --summary <text> [extra args]" >&2
  exit 1
fi
exec agentcodehandoff request --from-agent claude --to-agent codex "$@"
