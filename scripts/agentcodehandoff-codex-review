#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "usage: agentcodehandoff-codex-review --summary <text> [extra args]" >&2
  exit 1
fi
exec agentcodehandoff review --from-agent codex --to-agent hermes "$@"
