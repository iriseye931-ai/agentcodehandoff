#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "usage: agentcodehandoff-hermes-done --summary <text> [extra args]" >&2
  exit 1
fi
exec agentcodehandoff done --from-agent hermes --to-agent codex "$@"
