#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "usage: agentcodehandoff-codex-request --summary <text> [extra args]" >&2
  exit 1
fi
exec agentcodehandoff request --from-agent codex --to-agent hermes "$@"
