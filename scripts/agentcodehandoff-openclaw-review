#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "usage: agentcodehandoff-openclaw-review --summary <text> [extra args]" >&2
  exit 1
fi
exec agentcodehandoff send --from-agent openclaw --to-agent codex --role review "$@"
